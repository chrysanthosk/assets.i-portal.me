# ---------------------------------------------------------------------------
# Docker environment for assets.i-portal.me (used by docker-compose.yml)
#
#   cp .env.docker.example .env
#   docker compose up -d --build
#
# These values are read by Docker Compose for variable substitution and are
# passed into the containers. The app entrypoint generates APP_KEY for you.
# ---------------------------------------------------------------------------

APP_NAME=assets.i-portal.me
APP_ENV=production
APP_DEBUG=false
APP_URL=http://localhost:8080
# Timezone used for rent due dates and the daily reminder run
APP_TIMEZONE=Europe/Athens
# Encryption key. Leave blank: scripts/docker-preflight.sh (run by install.sh
# and new_deploy.sh) generates it once and saves it here. Never rotate it on a
# live install — 2FA secrets and recovery codes are encrypted with it.
APP_KEY=

# Host port the app is published on (http://localhost:<WEB_PORT>).
# If another stack already uses it, the preflight moves it to the next free port.
WEB_PORT=8080

# Database (a fresh MySQL "latest" container is provisioned automatically)
DB_DATABASE=assets
DB_USERNAME=assets
DB_PASSWORD=change_me_app
DB_ROOT_PASSWORD=change_me_root

# Laravel drivers
CACHE_STORE=database
SESSION_DRIVER=database
QUEUE_CONNECTION=database

# Admin auto-created on first boot. You log in with ADMIN_USERNAME (NOT the
# email). Leave EMAIL/PASSWORD blank to skip and create one manually later with
# `docker compose exec app php artisan make:admin`.
ADMIN_NAME=Admin
ADMIN_USERNAME=admin
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=ChangeMe123!

# Title deed import (optional here — you can paste the key in Settings → Portal instead)
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-opus-5
