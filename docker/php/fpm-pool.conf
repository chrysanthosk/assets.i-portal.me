[www]
; Forward PHP-FPM worker stdout/stderr to the master process so application
; logs (LOG_CHANNEL=stderr) surface via `docker compose logs`.
catch_workers_output = yes
decorate_workers_output = no
